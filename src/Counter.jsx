import React, { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  for (let i = 0; i < desInputStr.length; i++) {
    desInputStr.length;
    console.log(desInputStr.length);
  }
  /*   setCount(desInputStr.length); */
}

export default Counter;
